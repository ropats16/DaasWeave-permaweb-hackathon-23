import type { AppInfo, GatewayConfig, PermissionType } from "arconnect";
import BrowserWalletStrategy from "arweave-wallet-kit/dist/strategy/strategies/BrowserWallet";
import type Strategy from "arweave-wallet-kit/dist/strategy/Strategy";
export default class ArweaveWebWalletStrategy extends BrowserWalletStrategy implements Strategy {
    #private;
    id: "webwallet";
    name: string;
    description: string;
    theme: string;
    logo: string;
    url: string;
    constructor();
    isAvailable(): Promise<boolean>;
    resumeSession(): Promise<void>;
    connect(permissions: PermissionType[], appInfo?: AppInfo, gateway?: GatewayConfig): Promise<void>;
    addAddressEvent(listener: (address: string) => void): any;
    removeAddressEvent(listener: any): void;
}
