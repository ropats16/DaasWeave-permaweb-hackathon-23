import type { PostMessageOptions } from "../types";
export declare class PromiseController {
    private _promiseController;
    newMessagePromise(message: any, options?: PostMessageOptions): Promise<unknown>;
    processResponse(message: object): true | undefined;
}
