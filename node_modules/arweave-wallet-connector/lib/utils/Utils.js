export function generateUrl(url) {
    if (typeof url === 'object') {
        return url;
    }
    if (!url.includes('://')) {
        url = 'https://' + url;
    }
    return new URL(url);
}
//# sourceMappingURL=Utils.js.map