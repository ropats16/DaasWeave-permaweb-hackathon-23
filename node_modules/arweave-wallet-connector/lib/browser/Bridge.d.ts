import Emitter from 'arweave-wallet-connector/lib/utils/Emitter.js';
import type { AppInfo, PostMessageOptions } from 'arweave-wallet-connector/lib/types.js';
export declare type Emitting = {
    message: {
        method: string;
        params: unknown;
        session?: number | string | undefined;
    };
    builtin: {
        usePopup: boolean;
    } | {
        requirePopup: boolean;
    } | {
        keepPopup: boolean;
    } | {
        showIframe: boolean;
    };
};
export default class Bridge extends Emitter<Emitting> {
    private _url;
    private _iframeParentNode?;
    private _iframeNode?;
    private _iframeEl?;
    private _iframe;
    private _showIframe;
    private _popup;
    private _usePopup;
    private _requirePopup;
    private _keepPopup;
    private _promiseController;
    private _pending;
    get url(): string;
    get showIframe(): boolean;
    set showIframe(value: boolean);
    get usePopup(): boolean;
    private setUsePopup;
    get requirePopup(): boolean;
    private setRequirePopup;
    get keepPopup(): boolean;
    set keepPopup(value: boolean);
    constructor(connectToUrl: URL, appInfo?: AppInfo);
    destructor(options?: object): void;
    private listener;
    postMessage(message: object, options?: PostMessageOptions): Promise<unknown>;
    private openIframe;
    private closeIframe;
    private openPopup;
    private closePopup;
    completeRequest(): void;
    deliverMessage(message: any, options?: PostMessageOptions): void;
}
